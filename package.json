{
  "name": "bidpulse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Live rate card for pay-to-rank leaderboards. Real cost per click, and which boards have flatlined.",
  "scripts": {
    "dev": "npx --yes serve . -l 3000",
    "crawl": "node crawler.js",
    "crawl:watch": "node --watch crawler.js"
  },
  "engines": { "node": ">=20" }
}
